from database import*
from functions import*

#print (default_menu)

#close = {stall:['Store is Closed'] for (stall,menu) in default_menu.items()}
#print (close)

x = todaymenu('2')
print (x['chickenrice'])